<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ld.store.dao.UserInfoDao">
    <sql id="Base_Column_List">
            userid,
            username,
            userpassword,
            userpression,
            creattime,
            enabled
    </sql>
    <resultMap id="BaseResultMap" type="com.ld.store.entity.Userinfo">
        <result column="userid" property="userid"/>
        <result column="username" property="username"/>
        <result column="userpassword" property="userpassword"/>
        <result column="userpression" property="userpression"/>
        <result column="creattime" property="creattime"/>
        <result column="enabled" property="enabled"/>
    </resultMap>
    <insert id="addUserInfo" parameterType="com.ld.store.entity.Userinfo" >
    insert into userInfo(userId,userName ,userPassword,userPression  ,creatTime ,enabled)
        values (#{userInfo.userid},#{userInfo.username},#{userInfo.userpassword} ,#{userInfo.userpression},#{userInfo.creattime},#{userInfo.enabled})
    </insert>
    <select id="queryAllUserInfo" parameterType="int" resultType="com.ld.store.entity.Userinfo">
        select * from userInfo where enabled = 0 limit #{startRow} ,#{pageSize}
    </select>

    <update id="deleteUserInfo" parameterType="String">
        update userInfo set enabled = -1 where userId=#{userId,jdbcType=VARCHAR}
    </update>
    <select id="login" parameterType="String" resultType="com.ld.store.entity.Userinfo">
        select * from userInfo where userId = #{userId,jdbcType=VARCHAR}  and  userPassword = #{userPassword,jdbcType=VARCHAR}
    </select>
    <select id="queryUserInfoCount" resultType="int">
        select count(1) from userinfo u
    </select>

<!--auto generated by MybatisCodeHelper on 2019-12-19-->
    <select id="queryByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from userInfo
        where userid=#{userid}
    </select>
    <update id="updateUserInfoByUserId">
        update userinfo u set username=#{userName,jdbcType=VARCHAR} ,
                              userpassword=#{userPassword,jdbcType=VARCHAR}
        where userId =#{userId}
    </update>


</mapper>